/**
 * This test is a bit unclear what the task is. I have to look at the tests to understand
 * what the desired result is.
 */
// You can assume the input is an integer

// Average and worst case time complexity (Big-O) of my function is: O(1)
// Explain: Simple arithmatic operations.
function createBase(integer) {
  // TODO
  return (input) => {
    return integer + input
  }  
};

module.exports = createBase;
